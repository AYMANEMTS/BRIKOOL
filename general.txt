project BRIKOOL 
description: platform for Independent workers

conseption: 
    users types:
        admin (with permision) 
        workers

    authentication:
        admin,workers

    interface web:
        dashboard for administration (full acess to all systeme )
        dashboard for workers (browse his jobs, CRUD jobs)
        store for clients (browse jobs)

    interface mobile:
        dashboard for workers (browse his jobs, CRUD jobs)
        store for clients (browse jobs)

    tables:
        admins: (name, email, phone, cin, password)
        workers: (name, email, phone, cin, password, image, address)
        jobs: (image, name, description, contact, reviews, status, category_id, status)
        jobs_category: (name, image)
        comments: (cmnt, publisher, job_id)
        permisions: (name)
        permisions_admins 'pivot_table': ()
    
    relations:
        jobs (many to one) workers
        jobs_category (one to many) jobs
        comments (one to many) jobs
        permisions (many to many) admins




steps:
    website clients :
        home,workers list, worker detail, about us


idea: 
worker -> working time available 
first hero -> mobile app invite
	